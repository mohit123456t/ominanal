'use client';
import React from 'react';

const SupportView = ({ user, campaigns }: { user: any; campaigns: any[] }) => {
    return (
        <div className="p-6">
            <h2 className="text-2xl font-bold">Support</h2>
            <p>This is a placeholder for the support view.</p>
        </div>
    );
};

export default SupportView;
